<%= simple_form_for(@category) do |f| %>
      <%= f.input :name, placeholder: "Name"%>
      <%= f.input :description, placeholder: "Description" %>
      <%= f.input :photo, as: :file %>
      <%= f.submit "Add a new category", class: "btn btn-primary"%>
      <% end %>
